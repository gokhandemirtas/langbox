# MongoDB Configuration
MONGODB_USER="admin"
MONGODB_PASSWORD="admin"
MONGODB_DB="langbox"
MONGODB_PORT=27017
MONGODB_HOST="localhost"

# Qdrant (vector store for mem0 conversation memories + journal search)
QDRANT_HOST="localhost"
QDRANT_PORT=6333
QDRANT_GRPC_PORT=6334
# Optional: Full MongoDB URI (overrides individual settings above)
# MONGODB_URI=mongodb://admin:admin@localhost:27017

# Model Configuration
MODEL_PATH="models/"
MODEL_GENERALIST="llama-2-7b-chat.Q3_K_M.gguf"
MODEL_EMBEDDING="all-MiniLM-L6-v2-Q8_0.gguf"
EMBEDDING_DIM=384
MODEL_CTX=131072      # Context window size — set to model max if VRAM allows
MODEL_MAX_TOKENS=1024 # Max tokens for conversational responses
TELEGRAM_BOT_TOKEN=""          # from @BotFather
TELEGRAM_ALLOWED_CHAT_IDS=""  # your chat ID — get it from @userinfobot (comma-separated for multiple)

# OpenRouteService — directions and navigation (free at openrouteservice.org)
ORS_API_KEY=""
TAVILY_API_KEY=""

# Philips Hue Bridge Configuration (optional - only needed for home control features)
HUE_BRIDGE_IP="192.168.x.x"
# REQUESTS_CA_BUNDLE="hue_bridge.pem"  # Path to Hue bridge certificate if using HTTPS

# LangSmith Tracing Configuration (optional - for execution flow visualization)
LANGCHAIN_TRACING_V2=true
LANGCHAIN_ENDPOINT="https://api.smith.langchain.com"
LANGCHAIN_API_KEY="lsv2_pt_your_key_here"
LANGCHAIN_PROJECT="your_project_name"
# LANGSMITH_CERT_VERIFY=false  # Uncomment if having SSL certificate issues

SPOTIFY_CLIENT_ID=""                                                                                                                                  
SPOTIFY_CLIENT_SECRET=""